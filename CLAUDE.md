# Project Instructions

## Versioning

When the version in `.claude-plugin/plugin.json` is changed and pushed to main, always create a git tag matching the version (e.g., `git tag v1.2.1`) and push it (`git push origin --tags`).
