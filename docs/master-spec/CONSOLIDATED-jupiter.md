# Consolidated Spec: jupiter

Generated by jupiter:rewrite on 2026-04-20T05:05:23Z.
Source files are listed in the order shown below; originals preserved unless you chose cleanup at the end of this run.

<!-- source: plugins/jupiter/README.md -->
## README

## Jupiter

Consolidate scattered spec files into a canonical `docs/master-spec/` tree, emit a navigable `index.json`, and append stubs for undocumented public surface found in the code.

### Commands

#### `/jupiter:adopt [--force] [--deep]`

Primary command. Reorganizes every spec Jupiter can find into `docs/master-spec/`, organized by module (for multi-module repos) or feature (for single-module repos). Moves files with `git mv` so history is preserved.

- `--force`: wipe `docs/master-spec/` and rebuild (prompts first; default N).
- `--deep`: enable the optional behavioral-gaps pass (pass D) in the surface scanner. Off by default.

Re-running without `--force` is incremental: already-adopted specs are skipped; only new or changed specs are moved; surface stubs inside `<!-- jupiter:surface-begin -->` markers are regenerated; content outside those markers is preserved.

### `/jupiter:rewrite`

Consolidates the master spec into a single file (per module in multi-module mode) and prompts whether to delete the source files. Default answer is keep. Never deletes on blank input.

## Output layout

```
docs/master-spec/
  index.json                             # emitted by /jupiter:adopt
  <module>/<spec>.md                     # multi-module layout
  features/<feature>/<spec>.md           # single-module layout
  _surface.md                            # stubs that matched no existing spec
  CONSOLIDATED.md                        # emitted by /jupiter:rewrite (single-module)
  CONSOLIDATED-<module>.md               # emitted by /jupiter:rewrite (multi-module)
  consolidated-index.json                # rewrite sidecar
```

## Surface stub markers

Jupiter only rewrites content between these markers on incremental re-runs:

```
<!-- jupiter:surface-begin -->
## Undocumented surface (auto-generated)

- **name** (`location`) - summary <!-- TODO: expand -->

<!-- jupiter:surface-end -->
```

Anything above or below is never touched. To promote a stub into real prose, cut it out of the block, expand it above the opening marker, and it will survive future runs.

### Schema validation

Run the schema test after any schema change:

```bash
python plugins/jupiter/tests/validate_schemas.py
```

Expected: exit 0 with PASS lines for all four schemas.

<!-- source: plugins/jupiter/docs/superpowers/specs/2026-04-19-jupiter-plugin-design.md -->
## 2026-04-19-jupiter-plugin-design

## Jupiter Plugin - Design Spec

**Status:** Draft
**Date:** 2026-04-19
**Plugin:** `jupiter` (v0.1.0, to be created)
**Location:** `plugins/jupiter/` in this marketplace

### 1. Problem

Over the life of a project, design specs and implementation plans accumulate in scattered locations (`docs/`, `plugins/*/docs/`, per-module doc folders). In this repo we currently have roughly 4,600 lines of spec/plan content spread across:

- `plugins/qa-swarm/docs/MASTER-SPEC.md` (one plugin already consolidated)
- `plugins/qa-swarm/docs/superpowers/specs/*.md`
- `plugins/code-atlas/docs/*.md` (six separate design + reference files)
- `plugins/plan-runner/` (no consolidated spec; only skill markdown)
- `docs/plan-runner/2026-04-19/` and `docs/superpowers/specs/*.md`

New specs keep arriving in `docs/superpowers/specs/` every time a brainstorm session completes. The result: no single entry point for understanding what the system is supposed to do, and gaps between what the code does and what the specs describe.

Jupiter consolidates all scattered specs into a single canonical location, preserves them as the source of truth, generates an index for navigation, and scans the codebase to surface undocumented behavior.

### 2. Goals

1. Produce a single canonical spec tree at `docs/master-spec/`, organized to match the repo's structure.
2. Emit an `index.json` that makes the spec tree navigable and flags files that are getting too big.
3. Detect undocumented public surface and configuration, and inject stubs into the appropriate spec so gaps are impossible to miss.
4. Support incremental re-runs as new specs appear, without destroying manual edits.
5. Offer an alternative "rewrite" mode that produces a single consolidated document with an opt-in cleanup of the originals.

### 3. Non-goals

- Drift detection (specs say X, code does Y). The failure mode for this kind of check is too noisy to be worth the cost.
- Automated enforcement. Jupiter reports; it never blocks commits or fails CI.
- Owning content authorship. Jupiter moves and stubs; it does not rewrite the prose of existing specs.
- Multi-repo aggregation. Single-repo only.

### 4. Commands

Jupiter exposes two user-facing skills.

#### 4.1 `/jupiter:adopt`

The primary command. Reorganizes scattered specs into `docs/master-spec/`, generates the index, and appends undocumented-surface stubs.

**Flags:**
- `--force`: wipe `docs/master-spec/` and rebuild from scratch. Requires a single `y/N` confirmation before deletion.
- `--deep`: enable the optional D pass in the surface scanner (behavioral gaps). Off by default.

**Re-run behavior:** if `docs/master-spec/index.json` already exists, Jupiter runs in incremental mode (Section 9). Without `--force` it never deletes existing content.

#### 4.2 `/jupiter:rewrite`

Consolidates the master spec into a single document (per module in multi-module mode), and prompts the user whether to delete the source shards.

**Flags:** none.

**Behavior:** runs the same pipeline as adopt, then emits `CONSOLIDATED.md` (or `CONSOLIDATED-<module>.md` per module), writes a sidecar `consolidated-index.json`, and prompts `Delete the original spec files now? (y/N)`. The default is `N` - nothing destructive happens without explicit consent.

### 5. Module detection

Run once at the top of every invocation. A repo is **multi-module** if any of the following hold:

- Two or more `package.json` files exist outside the repo root.
- Two or more `plugin.json` files exist anywhere.
- A `pnpm-workspace.yaml`, `lerna.json`, or Cargo workspace with ≥2 members exists.
- Two or more top-level directories each contain their own package manifest.

Otherwise the repo is **single-module**.

Jupiter prints one log line stating which mode it picked and why (which signal triggered it). No prompt; no flag to override. If a user wants the other mode, they change their repo layout.

### 6. Output layout

```
docs/master-spec/
  index.json                           # always at this path
  <module>/                            # multi-module mode
    <spec-name>.md
    _surface.md                        # created only if no existing spec fits surface stubs
  features/<feature>/                  # single-module mode
    <spec-name>.md
  CONSOLIDATED.md                      # produced by /jupiter:rewrite
  CONSOLIDATED-<module>.md             # rewrite output, multi-module
  consolidated-index.json              # rewrite sidecar
```

File movement uses `git mv` so history is preserved. Jupiter never copies a spec - it always moves.

### 7. Pipeline

Identical shape for both commands; only the write phase differs.

```
orchestrator skill (jupiter:adopt | jupiter:rewrite)
  |
  +-- jupiter-spec-cataloger   (subagent, model: sonnet)
  |     Reads every .md under docs/ + plugins/*/docs/ + per-module doc dirs.
  |     Returns: specs catalog + proposed layout.
  |
  +-- jupiter-surface-scanner  (subagent, model: sonnet)
  |     Reads code per module. A+C pass always; D pass if --deep.
  |     Returns: undocumented-surface inventory per module.
  |
  +-- orchestrator write phase
        Adopt: git mv specs into buckets, append stubs, emit index.json, commit.
        Rewrite: concatenate specs into CONSOLIDATED.md, write consolidated-index.json,
                 prompt for cleanup, commit.
```

Only the orchestrator writes files. Subagents return JSON only; they do not modify the working tree.

### 8. Agent contracts

#### 8.1 `jupiter-spec-cataloger`

**Input fields (provided by orchestrator):**
- `mode`: `"module"` or `"feature"`
- `root`: absolute repo root
- `glob_targets`: spec file globs to crawl (`docs/**/*.md`, `plugins/*/docs/**/*.md`, root README files)
- `already_adopted` (incremental only): list of basenames already under `docs/master-spec/`

**For each file, the agent extracts:**
- file path (relative to root), LOC, last git modification time
- top-level headings with their line numbers and LOC
- 3-7 inferred topic keywords
- inferred module affiliation (for multi-module) or feature affiliation (for single-module)
- cross-references: mentions of other spec files by name or path

**Returns:**

```json
{
  "mode": "module",
  "specs": [
    {
      "path": "plugins/qa-swarm/docs/MASTER-SPEC.md",
      "loc": 690,
      "mtime": "2026-04-10T12:00:00Z",
      "topics": ["qa", "agents", "tdd", "aggregation"],
      "bucket": "qa-swarm",
      "headings": [
        { "heading": "## Architecture", "line": 42, "loc": 180 }
      ],
      "cross_refs": ["plugins/code-atlas/docs/2026-04-14-v1.2-design.md"]
    }
  ],
  "proposed_layout": {
    "qa-swarm": ["plugins/qa-swarm/docs/MASTER-SPEC.md", "..."],
    "code-atlas": ["..."],
    "plan-runner": ["..."]
  },
  "cross_refs": [
    { "from": "plugins/qa-swarm/docs/MASTER-SPEC.md", "to": "plugins/code-atlas/docs/..." }
  ]
}
```

Validated against `plugins/jupiter/schemas/catalog.schema.json`. On parse failure the orchestrator retries once with a reminder prompt; on second failure it prints raw output and STOPs.

#### 8.2 `jupiter-surface-scanner`

**Input fields:**
- `modules`: list of module paths from the cataloger
- `deep`: bool (true when `--deep` was set)

**Pass A - public surface (always):**
- exported functions/classes from entry files (`index.*`, `main.*`, module entry points)
- CLI scripts in `bin/`, `package.json#bin` entries
- Claude Code slash commands: `skills/*/SKILL.md` frontmatter `name`
- agent definitions: `agents/*.md` frontmatter `name`
- hook trigger names from `hooks/**/HOOK.md` frontmatter
- MCP tool names exported by the plugin

**Pass C - config/schema (always):**
- environment variable names in `.env.example` or `env.example`
- JSON schema files under `schemas/`
- non-default fields in `plugin.json`, `package.json`, `.claude-plugin/plugin.json`
- hook trigger conditions referenced in code but not in spec

**Pass D - behavioral gaps (only when `--deep`):**
- retry loops, fallbacks, error-recovery branches whose intent is not obvious from names
- nontrivial state machines or timeouts
- anything the agent tags as "code behavior unclear from names"

**Returns:**

```json
{
  "per_module": {
    "code-atlas": {
      "public_surface": [
        {
          "name": "atlas-dependencies",
          "kind": "agent",
          "location": "plugins/code-atlas/agents/atlas-dependencies.md",
          "one_line_summary": "Extracts module dependency graph from import statements",
          "confidence": "high"
        }
      ],
      "configs": [
        {
          "name": "CODE_ATLAS_MAX_DEPTH",
          "kind": "env_var",
          "location": ".env.example:12",
          "one_line_summary": "Maximum recursion depth for graph synthesis",
          "confidence": "high"
        }
      ],
      "behavioral_gaps": []
    }
  }
}
```

Validated against `plugins/jupiter/schemas/surface.schema.json`. Same retry/STOP policy as the cataloger.

### 9. Orchestrator write phase (adopt)

1. For each bucket in `proposed_layout`:
   1. Ensure the bucket directory exists.
   2. `git mv` each source spec into the bucket. If the destination path already exists, rename the incoming file with a `-N` suffix and log a warning.
   3. For each public-surface and config entry in that bucket's scanner output, pick the best-matching existing spec by **topic overlap** (count of shared keywords between the entry's inferred topics and the spec's topics; highest count wins; ties broken by smallest spec LOC). A spec must share **at least 2 topic keywords** to qualify. Append under a `## Undocumented surface (auto-generated)` heading bounded by:

      ```
      <!-- jupiter:surface-begin -->
      ## Undocumented surface (auto-generated)

      - **<name>** (`<location>`) - <one_line_summary> <!-- TODO: expand -->
      ...
      <!-- jupiter:surface-end -->
      ```

   4. If no existing spec qualifies (no spec shares at least 2 topic keywords), create `<bucket>/_surface.md` containing only the auto-generated section.
2. Write `docs/master-spec/index.json` (Section 10).
3. Stage all changes, commit with message `jupiter: adopt <N> specs into <B> buckets (mode: <module|feature>)`. If a pre-commit hook fails, surface the output and ask `Continue without committing? (Y/n)` - same pattern as plan-runner.

### 10. `index.json` schema

```json
{
  "generated_at": "2026-04-19T18:00:00Z",
  "jupiter_version": "0.1.0",
  "mode": "module",
  "root": "docs/master-spec/",
  "buckets": {
    "qa-swarm": {
      "summary": "Multi-agent QA analyzer with TDD fix pipeline",
      "files": [
        {
          "path": "qa-swarm/MASTER-SPEC.md",
          "loc": 814,
          "topics": ["agents", "bug-aggregation", "tdd"],
          "sections": [
            { "heading": "## Architecture", "line": 42, "loc": 180 }
          ],
          "split_candidate": true,
          "split_reason": "exceeds 800 LOC; 3 top-level sections >=150 LOC each",
          "cross_refs": ["../code-atlas/graph-language.md"]
        }
      ],
      "gaps_count": 7
    }
  },
  "split_candidates": ["qa-swarm/MASTER-SPEC.md"],
  "total_loc": 4603,
  "scan_summary": {
    "public_surface_found": 34,
    "configs_found": 12,
    "behavioral_gaps_found": 0
  }
}
```

**Split-candidate rule:** a file is flagged `split_candidate: true` if either:
- total LOC > 800, OR
- file has ≥3 top-level (`##`) sections each with ≥150 LOC

Thresholds are hardcoded constants in the orchestrator skill. No flags. If they prove wrong in practice, change the constants in a follow-up version.

### 11. Rewrite flow (`/jupiter:rewrite`)

1. Run cataloger + scanner (same as adopt). This works whether or not adopt has been run - rewrite is stand-alone.
2. Concatenate specs into the output file:
   - Multi-module: one file per module at `docs/master-spec/CONSOLIDATED-<module>.md`.
   - Single-module: one file at `docs/master-spec/CONSOLIDATED.md`.
3. Within each consolidated file:
   - Order: by bucket; within bucket, by **topic proximity** (specs are placed adjacent to whichever other spec in the same bucket shares the most topic keywords; ties broken by original file mtime, oldest first).
   - Each original file becomes an `<h2>` section with a `<!-- source: <original-path> -->` comment on the first line of the section for traceability.
   - Surface/config stubs appear inline per-bucket, in the same `<!-- jupiter:surface-begin -->` / `<!-- jupiter:surface-end -->` markers.
4. Write `docs/master-spec/consolidated-index.json` - same shape as `index.json` but `files[]` is replaced by `sections[]` pointing to heading anchors inside the consolidated file.
5. Prompt:

   ```
   Rewrite complete. <N> source files consolidated into <path> (<total_loc> LOC).
   Delete the original spec files now? (y/N)
   ```

   On `y`: `git rm` the originals and commit both the consolidated file and the deletions in a single commit (`jupiter: rewrite + cleanup <N> originals`).
   On `N` (default): commit only the new consolidated file (`jupiter: rewrite <N> specs (originals kept)`).

### 12. Incremental re-run model

When `/jupiter:adopt` runs and `docs/master-spec/index.json` already exists:

1. Build a set of `already_adopted` basenames from the existing index.
2. Pass that set to the cataloger, which reports only new/moved/changed specs (anything not in the set, plus any already-adopted file whose mtime is newer than the index `generated_at`).
3. Run the scanner unconditionally (code changes constantly, so the surface inventory is always re-derived).
4. Write phase:
   - Only `git mv` specs that are still outside `docs/master-spec/`.
   - For surface stubs: regenerate content between `<!-- jupiter:surface-begin -->` and `<!-- jupiter:surface-end -->` markers. Any content outside those markers is preserved, including prose that promotes a stub into real documentation above the auto-generated section.
5. Rewrite `index.json` from scratch using the fresh catalog + scan.
6. Commit.

`--force` deletes `docs/master-spec/` entirely after a single `y/N` confirmation, then runs as a fresh adopt.

### 13. Surface-stub idempotency

The auto-generated surface section is always bounded by matching markers:

```
<!-- jupiter:surface-begin -->
## Undocumented surface (auto-generated)

- **atlas-dependencies** (`plugins/code-atlas/agents/atlas-dependencies.md`) - Extracts module dependency graph from import statements <!-- TODO: expand -->

<!-- jupiter:surface-end -->
```

On re-run Jupiter replaces only the content between the markers. Anything a human wrote above `<!-- jupiter:surface-begin -->` or below `<!-- jupiter:surface-end -->` is never touched. This lets you promote a stub into polished prose by cutting it out of the marked block and expanding it above, knowing it will survive future runs.

If both markers are missing, Jupiter appends a new marked block at the end of the file. If only one marker is present, Jupiter treats the file as corrupt, logs a warning, and skips the stub append for that file.

### 14. Error handling

- **Agent returns non-JSON:** one retry with a reminder prompt. Second failure: print raw output, STOP.
- **Schema validation failure:** STOP; print the failing JSONPath and the offending value.
- **`git mv` destination collision:** rename incoming file with `-N` suffix (where N is the smallest integer that avoids collision); log a warning to the run summary.
- **Uncommitted working tree at start:** print `git status` output, warn that adopt commits on completion, prompt `Continue anyway? (Y/n)`. Same pattern as plan-runner.
- **Missing `docs/` directory:** create it silently. No error.
- **Pre-commit hook failure on final commit:** surface hook output, prompt `Continue without committing? (Y/n)`.
- **`--force` with no existing `master-spec/`:** treat as a no-op flag (don't error), proceed as a normal fresh adopt.

No retries beyond what is spelled out. Jupiter never masks a problem.

### 15. Testing

`plugins/jupiter/test-fixtures/` contains two fixture repos for manual smoke testing:

- **`fixture-multi-module/`** - mimics this marketplace: three fake plugins each with scattered specs in different locations. Verifies module-detection, layout, and `git mv`.
- **`fixture-single-module/`** - flat repo with ~5 loose specs in `docs/` and a `src/` tree with exports. Verifies feature-mode layout and surface scanning.

Manual test procedure documented in `plugins/jupiter/README.md` with expected outcomes:
- expected `index.json` bucket count and mode
- expected number of surface stubs
- expected commit messages

No automated test runner. Matches how `qa-swarm`, `code-atlas`, and `plan-runner` are tested in this repo.

### 16. Plugin layout

```
plugins/jupiter/
  .claude-plugin/
    plugin.json                         # v0.1.0, name: jupiter
  README.md                             # usage + manual test procedure
  LICENSE                               # MIT, matches siblings
  agents/
    jupiter-spec-cataloger.md
    jupiter-surface-scanner.md
  skills/
    adopt/SKILL.md                      # /jupiter:adopt
    rewrite/SKILL.md                    # /jupiter:rewrite
  schemas/
    catalog.schema.json
    surface.schema.json
    index.schema.json
    consolidated-index.schema.json
  test-fixtures/
    fixture-multi-module/
    fixture-single-module/
```

### 17. Tech stack alignment

Follows the conventions in `CLAUDE.md`:

- Agents: Markdown + YAML frontmatter (`name`, `description`, `model`, `color`)
- Skills: `skills/<name>/SKILL.md` with frontmatter; argument-hints where applicable
- Naming: kebab-case; skills as `jupiter:<name>`
- Models: Sonnet for both subagents (parity with code-atlas and plan-runner)
- Output: agents return JSON; skills produce Markdown + JSON artifacts
- License: MIT
- No runtime dependencies

### 18. Registration & versioning

1. Add `plugins/jupiter/` to `.claude-plugin/marketplace.json`.
2. Update root `README.md` and root `CLAUDE.md` "Current Plugins" table with the new entry.
3. Start at `v0.1.0`. Tag on first release as `jupiter/v0.1.0`.

### 19. Future considerations (explicitly out of scope for v0.1.0)

- Incremental `--deep` re-runs (currently `--deep` re-runs the full scan every time).
- Custom bucket overrides (user says "put everything with topic X in bucket Y").
- Non-markdown spec sources (ADRs in other formats, inline JSDoc comments).
- Multi-repo aggregation.
- Automatic split execution - currently Jupiter only flags split candidates; actually breaking a file up stays manual.

<!-- source: plugins/jupiter/docs/superpowers/plans/2026-04-19-jupiter-plugin.md -->
## 2026-04-19-jupiter-plugin

## Jupiter Plugin Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build the `jupiter` Claude Code plugin that adopts scattered spec files into a canonical `docs/master-spec/` tree, generates an index with split-candidate flags, and appends undocumented-surface stubs scanned from the codebase.

**Architecture:** Two user-facing skills (`/jupiter:adopt` and `/jupiter:rewrite`) orchestrate a pipeline of two Sonnet subagents (`jupiter-spec-cataloger`, `jupiter-surface-scanner`). Subagents return JSON; the skill does all file I/O. Pattern mirrors `code-atlas`.

**Tech Stack:** Markdown + YAML frontmatter for agents/skills, JSON Schema (draft-07) for I/O contracts, Python `jsonschema` package for local validation in tests, standard Claude Code plugin layout under `plugins/jupiter/`.

**Spec reference:** `docs/superpowers/specs/2026-04-19-jupiter-plugin-design.md`

---

### File Structure

Files created in this plan:

```
plugins/jupiter/
  .claude-plugin/
    plugin.json                          # Task 1
  README.md                              # Task 1 (stub) + Task 13 (full)
  LICENSE                                # Task 1 (copied from sibling)
  agents/
    jupiter-spec-cataloger.md            # Task 7
    jupiter-surface-scanner.md           # Task 8
  skills/
    adopt/SKILL.md                       # Task 9
    rewrite/SKILL.md                     # Task 10
  schemas/
    catalog.schema.json                  # Task 2
    surface.schema.json                  # Task 3
    index.schema.json                    # Task 4
    consolidated-index.schema.json       # Task 5
    examples/
      catalog-valid.json                 # Task 2
      catalog-invalid.json               # Task 2
      surface-valid.json                 # Task 3
      surface-invalid.json               # Task 3
      index-valid.json                   # Task 4
      index-invalid.json                 # Task 4
      consolidated-index-valid.json      # Task 5
      consolidated-index-invalid.json    # Task 5
  test-fixtures/
    fixture-multi-module/                # Task 11
    fixture-single-module/               # Task 12
  tests/
    validate_schemas.py                  # Task 2
```

Files modified in this plan:

```
.claude-plugin/marketplace.json          # Task 14
README.md                                # Task 14
CLAUDE.md                                # Task 14
```

---

### Task 1: Plugin skeleton

**Files:**
- Create: `plugins/jupiter/.claude-plugin/plugin.json`
- Create: `plugins/jupiter/LICENSE`
- Create: `plugins/jupiter/README.md`
- Create directory: `plugins/jupiter/agents/`
- Create directory: `plugins/jupiter/skills/`
- Create directory: `plugins/jupiter/schemas/`
- Create directory: `plugins/jupiter/schemas/examples/`
- Create directory: `plugins/jupiter/test-fixtures/`
- Create directory: `plugins/jupiter/tests/`

- [ ] **Step 1: Create the directory tree**

```bash
mkdir -p plugins/jupiter/.claude-plugin \
         plugins/jupiter/agents \
         plugins/jupiter/skills \
         plugins/jupiter/schemas/examples \
         plugins/jupiter/test-fixtures \
         plugins/jupiter/tests
```

- [ ] **Step 2: Write `plugins/jupiter/.claude-plugin/plugin.json`**

```json
{
  "name": "jupiter",
  "description": "Consolidate scattered specs into a canonical master-spec tree with a navigable index, and append stubs for undocumented public surface found in code",
  "version": "0.1.0",
  "license": "MIT",
  "keywords": [
    "specs",
    "documentation",
    "consolidation",
    "index",
    "codebase-scan",
    "developer-tools"
  ],
  "repository": "https://github.com/MisterVitoPro/qa-claude-market"
}
```

- [ ] **Step 3: Copy the LICENSE file from a sibling plugin**

```bash
cp plugins/plan-runner/LICENSE plugins/jupiter/LICENSE
```

- [ ] **Step 4: Write stub `plugins/jupiter/README.md`**

```markdown
# Jupiter

Consolidate scattered spec files into a canonical master-spec tree with an index, and scan the codebase for undocumented public surface.

Status: pre-release (v0.1.0). Full usage guide added after skills are implemented.
```

- [ ] **Step 5: Validate the manifest JSON parses**

```bash
python -c "import json; json.load(open('plugins/jupiter/.claude-plugin/plugin.json'))" && echo OK
```

Expected output: `OK`

- [ ] **Step 6: Commit**

```bash
git add plugins/jupiter/
git commit -m "jupiter: scaffold plugin skeleton"
```

---

### Task 2: catalog.schema.json

**Files:**
- Create: `plugins/jupiter/schemas/catalog.schema.json`
- Create: `plugins/jupiter/schemas/examples/catalog-valid.json`
- Create: `plugins/jupiter/schemas/examples/catalog-invalid.json`
- Create: `plugins/jupiter/tests/validate_schemas.py`

- [ ] **Step 1: Write the failing test**

`plugins/jupiter/tests/validate_schemas.py`:

```python
"""Validate every Jupiter schema against its valid and invalid example fixtures.

Exit 0 on success. Exit 1 if any valid example fails or any invalid example passes.
"""

import json
import sys
from pathlib import Path

import jsonschema

ROOT = Path(__file__).resolve().parent.parent
SCHEMAS_DIR = ROOT / "schemas"
EXAMPLES_DIR = SCHEMAS_DIR / "examples"

CASES = [
    ("catalog.schema.json", "catalog-valid.json", "catalog-invalid.json"),
    ("surface.schema.json", "surface-valid.json", "surface-invalid.json"),
    ("index.schema.json", "index-valid.json", "index-invalid.json"),
    (
        "consolidated-index.schema.json",
        "consolidated-index-valid.json",
        "consolidated-index-invalid.json",
    ),
]


def load(p: Path):
    with p.open("r", encoding="utf-8") as f:
        return json.load(f)


def main() -> int:
    failures = []
    for schema_name, valid_name, invalid_name in CASES:
        schema_path = SCHEMAS_DIR / schema_name
        valid_path = EXAMPLES_DIR / valid_name
        invalid_path = EXAMPLES_DIR / invalid_name
        if not schema_path.exists():
            print(f"SKIP: {schema_name} not yet written")
            continue
        schema = load(schema_path)
        try:
            jsonschema.validate(load(valid_path), schema)
            print(f"PASS: {valid_name} validates against {schema_name}")
        except jsonschema.ValidationError as e:
            failures.append(f"FAIL: {valid_name} should validate: {e.message}")
        try:
            jsonschema.validate(load(invalid_path), schema)
            failures.append(f"FAIL: {invalid_name} should NOT validate")
        except jsonschema.ValidationError:
            print(f"PASS: {invalid_name} correctly rejected by {schema_name}")
    if failures:
        for f in failures:
            print(f)
        return 1
    return 0


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Step 2: Run the test — should skip because no schema exists yet**

```bash
python plugins/jupiter/tests/validate_schemas.py
```

Expected: `SKIP:` lines for all four schemas, exit 0.

- [ ] **Step 3: Write `plugins/jupiter/schemas/examples/catalog-valid.json`**

```json
{
  "mode": "module",
  "specs": [
    {
      "path": "plugins/qa-swarm/docs/MASTER-SPEC.md",
      "loc": 690,
      "mtime": "2026-04-10T12:00:00Z",
      "topics": ["qa", "agents", "tdd", "aggregation"],
      "bucket": "qa-swarm",
      "headings": [
        { "heading": "## Architecture", "line": 42, "loc": 180 }
      ],
      "cross_refs": ["plugins/code-atlas/docs/2026-04-14-v1.2-design.md"]
    }
  ],
  "proposed_layout": {
    "qa-swarm": ["plugins/qa-swarm/docs/MASTER-SPEC.md"]
  },
  "cross_refs": [
    {
      "from": "plugins/qa-swarm/docs/MASTER-SPEC.md",
      "to": "plugins/code-atlas/docs/2026-04-14-v1.2-design.md"
    }
  ]
}
```

- [ ] **Step 4: Write `plugins/jupiter/schemas/examples/catalog-invalid.json`**

Missing the required `mode` field and uses a non-enum value for what would be mode.

```json
{
  "specs": [],
  "proposed_layout": {},
  "cross_refs": []
}
```

- [ ] **Step 5: Write `plugins/jupiter/schemas/catalog.schema.json`**

```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Jupiter Catalog",
  "type": "object",
  "required": ["mode", "specs", "proposed_layout", "cross_refs"],
  "additionalProperties": false,
  "properties": {
    "mode": { "enum": ["module", "feature"] },
    "specs": {
      "type": "array",
      "items": {
        "type": "object",
        "required": [
          "path",
          "loc",
          "mtime",
          "topics",
          "bucket",
          "headings",
          "cross_refs"
        ],
        "additionalProperties": false,
        "properties": {
          "path": { "type": "string" },
          "loc": { "type": "integer", "minimum": 0 },
          "mtime": { "type": "string", "format": "date-time" },
          "topics": {
            "type": "array",
            "items": { "type": "string" },
            "minItems": 3,
            "maxItems": 7
          },
          "bucket": { "type": "string" },
          "headings": {
            "type": "array",
            "items": {
              "type": "object",
              "required": ["heading", "line", "loc"],
              "additionalProperties": false,
              "properties": {
                "heading": { "type": "string" },
                "line": { "type": "integer", "minimum": 1 },
                "loc": { "type": "integer", "minimum": 0 }
              }
            }
          },
          "cross_refs": { "type": "array", "items": { "type": "string" } }
        }
      }
    },
    "proposed_layout": {
      "type": "object",
      "additionalProperties": {
        "type": "array",
        "items": { "type": "string" }
      }
    },
    "cross_refs": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["from", "to"],
        "additionalProperties": false,
        "properties": {
          "from": { "type": "string" },
          "to": { "type": "string" }
        }
      }
    }
  }
}
```

- [ ] **Step 6: Run the test — valid should pass, invalid should be rejected**

```bash
python plugins/jupiter/tests/validate_schemas.py
```

Expected:
```
PASS: catalog-valid.json validates against catalog.schema.json
PASS: catalog-invalid.json correctly rejected by catalog.schema.json
SKIP: surface.schema.json not yet written
SKIP: index.schema.json not yet written
SKIP: consolidated-index.schema.json not yet written
```

Exit 0.

- [ ] **Step 7: Commit**

```bash
git add plugins/jupiter/schemas/ plugins/jupiter/tests/
git commit -m "jupiter: add catalog.schema.json with validation test"
```

---

### Task 3: surface.schema.json

**Files:**
- Create: `plugins/jupiter/schemas/surface.schema.json`
- Create: `plugins/jupiter/schemas/examples/surface-valid.json`
- Create: `plugins/jupiter/schemas/examples/surface-invalid.json`

- [ ] **Step 1: Write `plugins/jupiter/schemas/examples/surface-valid.json`**

```json
{
  "per_module": {
    "code-atlas": {
      "public_surface": [
        {
          "name": "atlas-dependencies",
          "kind": "agent",
          "location": "plugins/code-atlas/agents/atlas-dependencies.md",
          "one_line_summary": "Extracts module dependency graph from import statements",
          "confidence": "high"
        }
      ],
      "configs": [
        {
          "name": "CODE_ATLAS_MAX_DEPTH",
          "kind": "env_var",
          "location": ".env.example:12",
          "one_line_summary": "Maximum recursion depth for graph synthesis",
          "confidence": "high"
        }
      ],
      "behavioral_gaps": []
    }
  }
}
```

- [ ] **Step 2: Write `plugins/jupiter/schemas/examples/surface-invalid.json`**

Missing required `confidence` field on the entry, and uses an unknown top-level key.

```json
{
  "per_module": {
    "code-atlas": {
      "public_surface": [
        {
          "name": "atlas-dependencies",
          "kind": "agent",
          "location": "plugins/code-atlas/agents/atlas-dependencies.md",
          "one_line_summary": "Extracts module dependency graph from import statements"
        }
      ],
      "configs": [],
      "behavioral_gaps": []
    }
  },
  "unknown_field": true
}
```

- [ ] **Step 3: Write `plugins/jupiter/schemas/surface.schema.json`**

```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Jupiter Surface Inventory",
  "type": "object",
  "required": ["per_module"],
  "additionalProperties": false,
  "properties": {
    "per_module": {
      "type": "object",
      "additionalProperties": {
        "type": "object",
        "required": ["public_surface", "configs", "behavioral_gaps"],
        "additionalProperties": false,
        "properties": {
          "public_surface": { "$ref": "#/definitions/entry_array" },
          "configs": { "$ref": "#/definitions/entry_array" },
          "behavioral_gaps": { "$ref": "#/definitions/entry_array" }
        }
      }
    }
  },
  "definitions": {
    "entry_array": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["name", "kind", "location", "one_line_summary", "confidence"],
        "additionalProperties": false,
        "properties": {
          "name": { "type": "string" },
          "kind": { "type": "string" },
          "location": { "type": "string" },
          "one_line_summary": { "type": "string" },
          "confidence": { "enum": ["high", "low"] }
        }
      }
    }
  }
}
```

- [ ] **Step 4: Run the validation test**

```bash
python plugins/jupiter/tests/validate_schemas.py
```

Expected: PASS for both catalog and surface examples, SKIP for index and consolidated-index.

- [ ] **Step 5: Commit**

```bash
git add plugins/jupiter/schemas/
git commit -m "jupiter: add surface.schema.json with validation test"
```

---

### Task 4: index.schema.json

**Files:**
- Create: `plugins/jupiter/schemas/index.schema.json`
- Create: `plugins/jupiter/schemas/examples/index-valid.json`
- Create: `plugins/jupiter/schemas/examples/index-invalid.json`

- [ ] **Step 1: Write `plugins/jupiter/schemas/examples/index-valid.json`**

```json
{
  "generated_at": "2026-04-19T18:00:00Z",
  "jupiter_version": "0.1.0",
  "mode": "module",
  "root": "docs/master-spec/",
  "buckets": {
    "qa-swarm": {
      "summary": "Multi-agent QA analyzer with TDD fix pipeline",
      "files": [
        {
          "path": "qa-swarm/MASTER-SPEC.md",
          "loc": 814,
          "topics": ["agents", "bug-aggregation", "tdd"],
          "sections": [
            { "heading": "## Architecture", "line": 42, "loc": 180 }
          ],
          "split_candidate": true,
          "split_reason": "exceeds 800 LOC; 3 top-level sections >=150 LOC each",
          "cross_refs": ["../code-atlas/graph-language.md"]
        }
      ],
      "gaps_count": 7
    }
  },
  "split_candidates": ["qa-swarm/MASTER-SPEC.md"],
  "total_loc": 4603,
  "scan_summary": {
    "public_surface_found": 34,
    "configs_found": 12,
    "behavioral_gaps_found": 0
  }
}
```

- [ ] **Step 2: Write `plugins/jupiter/schemas/examples/index-invalid.json`**

Uses a bad `mode` enum value.

```json
{
  "generated_at": "2026-04-19T18:00:00Z",
  "jupiter_version": "0.1.0",
  "mode": "hybrid",
  "root": "docs/master-spec/",
  "buckets": {},
  "split_candidates": [],
  "total_loc": 0,
  "scan_summary": {
    "public_surface_found": 0,
    "configs_found": 0,
    "behavioral_gaps_found": 0
  }
}
```

- [ ] **Step 3: Write `plugins/jupiter/schemas/index.schema.json`**

```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Jupiter Master Spec Index",
  "type": "object",
  "required": [
    "generated_at",
    "jupiter_version",
    "mode",
    "root",
    "buckets",
    "split_candidates",
    "total_loc",
    "scan_summary"
  ],
  "additionalProperties": false,
  "properties": {
    "generated_at": { "type": "string", "format": "date-time" },
    "jupiter_version": { "type": "string" },
    "mode": { "enum": ["module", "feature"] },
    "root": { "type": "string" },
    "buckets": {
      "type": "object",
      "additionalProperties": {
        "type": "object",
        "required": ["summary", "files", "gaps_count"],
        "additionalProperties": false,
        "properties": {
          "summary": { "type": "string" },
          "files": {
            "type": "array",
            "items": {
              "type": "object",
              "required": [
                "path",
                "loc",
                "topics",
                "sections",
                "split_candidate",
                "cross_refs"
              ],
              "additionalProperties": false,
              "properties": {
                "path": { "type": "string" },
                "loc": { "type": "integer", "minimum": 0 },
                "topics": { "type": "array", "items": { "type": "string" } },
                "sections": {
                  "type": "array",
                  "items": {
                    "type": "object",
                    "required": ["heading", "line", "loc"],
                    "additionalProperties": false,
                    "properties": {
                      "heading": { "type": "string" },
                      "line": { "type": "integer", "minimum": 1 },
                      "loc": { "type": "integer", "minimum": 0 }
                    }
                  }
                },
                "split_candidate": { "type": "boolean" },
                "split_reason": { "type": "string" },
                "cross_refs": { "type": "array", "items": { "type": "string" } }
              }
            }
          },
          "gaps_count": { "type": "integer", "minimum": 0 }
        }
      }
    },
    "split_candidates": { "type": "array", "items": { "type": "string" } },
    "total_loc": { "type": "integer", "minimum": 0 },
    "scan_summary": {
      "type": "object",
      "required": [
        "public_surface_found",
        "configs_found",
        "behavioral_gaps_found"
      ],
      "additionalProperties": false,
      "properties": {
        "public_surface_found": { "type": "integer", "minimum": 0 },
        "configs_found": { "type": "integer", "minimum": 0 },
        "behavioral_gaps_found": { "type": "integer", "minimum": 0 }
      }
    }
  }
}
```

- [ ] **Step 4: Run the validation test**

```bash
python plugins/jupiter/tests/validate_schemas.py
```

Expected: PASS for catalog, surface, and index. SKIP for consolidated-index.

- [ ] **Step 5: Commit**

```bash
git add plugins/jupiter/schemas/
git commit -m "jupiter: add index.schema.json with validation test"
```

---

### Task 5: consolidated-index.schema.json

**Files:**
- Create: `plugins/jupiter/schemas/consolidated-index.schema.json`
- Create: `plugins/jupiter/schemas/examples/consolidated-index-valid.json`
- Create: `plugins/jupiter/schemas/examples/consolidated-index-invalid.json`

- [ ] **Step 1: Write `plugins/jupiter/schemas/examples/consolidated-index-valid.json`**

```json
{
  "generated_at": "2026-04-19T18:00:00Z",
  "jupiter_version": "0.1.0",
  "mode": "module",
  "root": "docs/master-spec/",
  "consolidated_files": ["CONSOLIDATED-qa-swarm.md", "CONSOLIDATED-code-atlas.md"],
  "buckets": {
    "qa-swarm": {
      "summary": "Multi-agent QA analyzer",
      "source_file": "CONSOLIDATED-qa-swarm.md",
      "sections": [
        {
          "anchor": "#master-spec",
          "original_path": "plugins/qa-swarm/docs/MASTER-SPEC.md",
          "heading": "## MASTER-SPEC",
          "line": 12,
          "loc": 690,
          "topics": ["qa", "agents", "tdd"]
        }
      ],
      "gaps_count": 7
    }
  },
  "total_loc": 4603,
  "scan_summary": {
    "public_surface_found": 34,
    "configs_found": 12,
    "behavioral_gaps_found": 0
  }
}
```

- [ ] **Step 2: Write `plugins/jupiter/schemas/examples/consolidated-index-invalid.json`**

Bucket missing the required `source_file` field.

```json
{
  "generated_at": "2026-04-19T18:00:00Z",
  "jupiter_version": "0.1.0",
  "mode": "feature",
  "root": "docs/master-spec/",
  "consolidated_files": ["CONSOLIDATED.md"],
  "buckets": {
    "main": {
      "summary": "Main feature area",
      "sections": [],
      "gaps_count": 0
    }
  },
  "total_loc": 0,
  "scan_summary": {
    "public_surface_found": 0,
    "configs_found": 0,
    "behavioral_gaps_found": 0
  }
}
```

- [ ] **Step 3: Write `plugins/jupiter/schemas/consolidated-index.schema.json`**

```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Jupiter Consolidated Spec Index",
  "type": "object",
  "required": [
    "generated_at",
    "jupiter_version",
    "mode",
    "root",
    "consolidated_files",
    "buckets",
    "total_loc",
    "scan_summary"
  ],
  "additionalProperties": false,
  "properties": {
    "generated_at": { "type": "string", "format": "date-time" },
    "jupiter_version": { "type": "string" },
    "mode": { "enum": ["module", "feature"] },
    "root": { "type": "string" },
    "consolidated_files": {
      "type": "array",
      "items": { "type": "string" },
      "minItems": 1
    },
    "buckets": {
      "type": "object",
      "additionalProperties": {
        "type": "object",
        "required": ["summary", "source_file", "sections", "gaps_count"],
        "additionalProperties": false,
        "properties": {
          "summary": { "type": "string" },
          "source_file": { "type": "string" },
          "sections": {
            "type": "array",
            "items": {
              "type": "object",
              "required": [
                "anchor",
                "original_path",
                "heading",
                "line",
                "loc",
                "topics"
              ],
              "additionalProperties": false,
              "properties": {
                "anchor": { "type": "string" },
                "original_path": { "type": "string" },
                "heading": { "type": "string" },
                "line": { "type": "integer", "minimum": 1 },
                "loc": { "type": "integer", "minimum": 0 },
                "topics": { "type": "array", "items": { "type": "string" } }
              }
            }
          },
          "gaps_count": { "type": "integer", "minimum": 0 }
        }
      }
    },
    "total_loc": { "type": "integer", "minimum": 0 },
    "scan_summary": {
      "type": "object",
      "required": [
        "public_surface_found",
        "configs_found",
        "behavioral_gaps_found"
      ],
      "additionalProperties": false,
      "properties": {
        "public_surface_found": { "type": "integer", "minimum": 0 },
        "configs_found": { "type": "integer", "minimum": 0 },
        "behavioral_gaps_found": { "type": "integer", "minimum": 0 }
      }
    }
  }
}
```

- [ ] **Step 4: Run the validation test**

```bash
python plugins/jupiter/tests/validate_schemas.py
```

Expected: PASS for all four schemas' valid examples, PASS (rejection) for all four invalid examples. Exit 0.

- [ ] **Step 5: Commit**

```bash
git add plugins/jupiter/schemas/
git commit -m "jupiter: add consolidated-index.schema.json with validation test"
```

---

### Task 6: Cataloger agent prompt

**Files:**
- Create: `plugins/jupiter/agents/jupiter-spec-cataloger.md`

Reference: sibling agents `plugins/plan-runner/agents/plan-analyzer.md` (same shape: read inputs, return strict JSON, no prose).

- [ ] **Step 1: Write the full agent file**

Write this file exactly:

````markdown
---
name: jupiter-spec-cataloger
description: >
  Jupiter pipeline agent that reads every spec file under a repo's docs/ and
  per-module doc dirs, extracts path/loc/mtime/headings/topics, proposes a
  module-or-feature bucket layout, and returns a strict JSON catalog for the
  orchestrator to act on.
model: sonnet
color: purple
---

You are the Jupiter Spec Cataloger. Your job: read every spec file a repo contains and emit a strict JSON catalog. You do NOT move, modify, or create any files -- you only read and report.

## Input

You receive:

1. `mode`: either `"module"` or `"feature"`. The orchestrator has already run module detection.
2. `root`: absolute repo root.
3. `glob_targets`: a list of glob patterns listing where spec files live. Typical set: `docs/**/*.md`, `plugins/*/docs/**/*.md`, root-level `README.md` plus any `plugins/*/README.md`.
4. `already_adopted` (optional): list of spec basenames (filename only, no path) already tracked in `docs/master-spec/index.json`. When provided, scope your output to new/moved/changed specs: a file counts as "changed" if its git mtime is newer than the timestamp passed as `index_generated_at`.
5. `index_generated_at` (optional): ISO 8601 timestamp from the existing `docs/master-spec/index.json`. Only used when `already_adopted` is provided.

The orchestrator inlines these fields into your prompt as JSON. Treat the list as authoritative -- do not scan outside the provided globs.

## What to extract per spec

For each spec file under the glob targets (minus any whose basename is in `already_adopted` and whose git mtime is older than `index_generated_at`):

- `path` -- relative to `root`, forward slashes.
- `loc` -- integer line count.
- `mtime` -- git modification time (`git log -1 --format=%cI <path>`). If the file is untracked, use the filesystem mtime formatted as ISO 8601.
- `topics` -- 3 to 7 inferred topic keywords. Derive from headings and the first 40 lines of prose. Keywords are single words or hyphenated compounds, lowercase.
- `bucket` -- the target bucket name:
  - In `module` mode: the name of the owning module, derived from the path (e.g., `plugins/qa-swarm/docs/X.md` -> `qa-swarm`). Root-level docs go into a synthetic `shared` bucket.
  - In `feature` mode: a keyword derived from the spec's dominant topic. Prefer reusing an existing bucket name from earlier specs if topic overlap is >=2 keywords; otherwise mint a new bucket.
- `headings` -- list of `{"heading": "## Title", "line": <line>, "loc": <lines-until-next-heading-or-eof>}`, for every `##`-level heading. Skip `#` (title) and `###`+.
- `cross_refs` -- list of other spec paths this file mentions by name or path. Match against the set of all spec paths found in this run.

## What to produce

You MUST return a single JSON object matching `plugins/jupiter/schemas/catalog.schema.json`. No prose, no Markdown fences -- just the JSON object.

Fields:

- `mode`: echo the input mode.
- `specs`: array of spec records (above).
- `proposed_layout`: `{"<bucket>": ["<spec path>", ...]}` -- reverse index of `specs[].bucket`.
- `cross_refs`: flattened `[{"from": "<path>", "to": "<path>"}, ...]` across all spec records.

## Output rules

- Return ONLY a JSON object. No Markdown code fences. No preface. No trailing commentary.
- All `path` values use forward slashes and are relative to `root`.
- If you cannot read a file (permission error, binary content), skip it silently -- do not include it in `specs`.
- If the repo contains zero matching spec files, return `{"mode": "<mode>", "specs": [], "proposed_layout": {}, "cross_refs": []}`.
- Do not invent specs you did not see. Every entry in `proposed_layout` must correspond to a `specs[]` entry.

## Sibling reference

The style and I/O discipline of this agent matches `plugins/plan-runner/agents/plan-analyzer.md`. When in doubt about tone or structure, consult that file.
````

- [ ] **Step 2: Verify frontmatter parses as YAML**

```bash
python -c "import yaml,re; t=open('plugins/jupiter/agents/jupiter-spec-cataloger.md').read(); m=re.match(r'^---\n(.*?)\n---', t, re.S); print(yaml.safe_load(m.group(1)))"
```

Expected: a dict with keys `name`, `description`, `model`, `color`. No exception.

- [ ] **Step 3: Commit**

```bash
git add plugins/jupiter/agents/jupiter-spec-cataloger.md
git commit -m "jupiter: add spec-cataloger agent"
```

---

### Task 7: Surface scanner agent prompt

**Files:**
- Create: `plugins/jupiter/agents/jupiter-surface-scanner.md`

- [ ] **Step 1: Write the full agent file**

````markdown
---
name: jupiter-surface-scanner
description: >
  Jupiter pipeline agent that scans a repo's code per module to inventory public
  surface (agents, skills, CLI commands, exports, hooks, MCP tools) and
  configuration (env vars, schemas, manifest fields). Optionally performs a
  deeper pass over nontrivial behavior. Returns strict JSON for the orchestrator.
model: sonnet
color: cyan
---

You are the Jupiter Surface Scanner. Your job: scan code under each module and emit a strict JSON inventory of what exists but may not be documented. You do NOT read any spec file, move files, or modify the working tree -- you only read code.

## Input

You receive:

1. `modules`: list of objects `{"name": "<bucket>", "path": "<repo-relative dir>"}`. These are the module boundaries the orchestrator wants you to scan. In multi-module mode this is the set of per-plugin or per-package roots. In feature mode this is a single entry covering the repo root.
2. `deep`: boolean. When `true`, also produce the `behavioral_gaps` pass. When `false`, return an empty array for `behavioral_gaps` in every module.
3. `root`: absolute repo root.

## Pass A -- public surface (always)

For each module, inventory:

- exported functions and classes from entry files (`index.*`, `main.*`, entries listed in `package.json#main` / `exports`)
- CLI scripts: files under `bin/`, entries in `package.json#bin`, shebanged scripts under `scripts/`
- Claude Code agents: `agents/*.md` -- extract the `name` field from YAML frontmatter
- Claude Code skills: `skills/**/SKILL.md` -- extract the `name` field from YAML frontmatter
- Claude Code hooks: `hooks/**/HOOK.md` -- extract the `trigger` field
- MCP tool names: look for `mcp__*__*` symbols or `tools: [...]` declarations in any server config

## Pass C -- config/schema (always)

For each module, inventory:

- environment variables: keys in `.env.example`, `env.example`, or any file matching `*.env.example`
- JSON schema files under `schemas/` (list by filename; one entry per file)
- non-default fields in `plugin.json`, `package.json`, `.claude-plugin/plugin.json` -- non-default means any field beyond `name`, `version`, `description`, `license`
- hook trigger conditions referenced in code but not declared in any `HOOK.md`

## Pass D -- behavioral gaps (only when `deep: true`)

For each module, when explicitly enabled:

- retry loops, fallbacks, error-recovery branches whose intent is not obvious from their function or variable names
- nontrivial state machines (3+ named states)
- timeouts or polling intervals with no accompanying comment explaining the chosen value
- anything else you would tag as "code behavior unclear from names"

Tag every behavioral-gap entry with `confidence: "low"`. Pass A and C entries are always `confidence: "high"`.

## Per-entry shape

```json
{
  "name": "<short identifier -- agent name, env var, function name, etc.>",
  "kind": "<agent | skill | hook | cli | export | env_var | schema | manifest_field | behavioral_gap>",
  "location": "<repo-relative path, optionally with :line suffix>",
  "one_line_summary": "<<=100 char description, inferred from code context>",
  "confidence": "high | low"
}
```

## What to produce

Return a single JSON object matching `plugins/jupiter/schemas/surface.schema.json`:

```json
{
  "per_module": {
    "<bucket>": {
      "public_surface": [<entry>, ...],
      "configs": [<entry>, ...],
      "behavioral_gaps": [<entry>, ...]
    }
  }
}
```

## Output rules

- Return ONLY the JSON object. No Markdown code fences. No preface. No trailing commentary.
- Every bucket provided in `modules` MUST appear in `per_module`, even if all three arrays are empty.
- Do not duplicate entries within a bucket. If the same symbol appears in two places, pick the primary location and note the alternates in `one_line_summary` ("primary X:N; also Y:N").
- When `deep: false`, every `behavioral_gaps` array MUST be `[]`.
- Never emit an entry you cannot ground in a file the orchestrator can open at the reported `location`.
````

- [ ] **Step 2: Verify frontmatter parses as YAML**

```bash
python -c "import yaml,re; t=open('plugins/jupiter/agents/jupiter-surface-scanner.md').read(); m=re.match(r'^---\n(.*?)\n---', t, re.S); print(yaml.safe_load(m.group(1)))"
```

Expected: a dict with keys `name`, `description`, `model`, `color`. No exception.

- [ ] **Step 3: Commit**

```bash
git add plugins/jupiter/agents/jupiter-surface-scanner.md
git commit -m "jupiter: add surface-scanner agent"
```

---

### Task 8: Adopt skill

**Files:**
- Create: `plugins/jupiter/skills/adopt/SKILL.md`

Reference: `plugins/plan-runner/skills/run/SKILL.md` (same shape: step-by-step orchestration, calls two subagents, writes artifacts, commits).

- [ ] **Step 1: Write the full skill file**

````markdown
---
name: jupiter:adopt
description: >
  Adopt scattered spec files into a canonical docs/master-spec/ tree organized
  by module (multi-module repos) or feature (single-module). Emits index.json
  with split-candidate flags and appends auto-generated stubs for undocumented
  public surface found in code. Incremental on re-run; --force wipes first.
argument-hint: "[--force] [--deep]"
---

You are orchestrating the Jupiter adopt pipeline. Follow these steps exactly.

## Step 0: Parse arguments

Parse `$ARGUMENTS`:
- `--force`: set `force = true`, otherwise `false`.
- `--deep`: set `deep = true`, otherwise `false`.
- Any other token: print usage and STOP.

Usage:
```
/jupiter:adopt [--force] [--deep]
  --force   Delete docs/master-spec/ and rebuild from scratch (prompts first).
  --deep    Enable behavioral-gaps pass (pass D) in the surface scanner.
```

## Step 1: Pre-flight

### 1a. Working tree check

Run `git status --porcelain`. If output is non-empty:

```
Warning: working tree has uncommitted changes:
<output>

Jupiter commits on completion. Recommend: commit or stash first.
Continue anyway? (Y/n)
```

Default N. On N, STOP.

### 1b. Detect incremental vs fresh

Check whether `docs/master-spec/index.json` exists.

- If yes and `force == false`: set `incremental = true`; read the existing index, extract `generated_at` into `index_generated_at`, and extract the set of adopted basenames by walking every `buckets[*].files[].path` (the last path segment is the basename).
- If yes and `force == true`: prompt

  ```
  --force will delete docs/master-spec/ and rebuild from scratch.
  All manually edited content inside that tree will be lost.
  Delete and rebuild? (y/N)
  ```

  Default N. On N, STOP. On y, `rm -rf docs/master-spec/` and proceed with `incremental = false`.
- If no and `force == true`: print `--force: no existing master-spec to wipe; proceeding as fresh adopt.` and set `incremental = false`.
- If no and `force == false`: `incremental = false`.

### 1c. Ensure `docs/` exists

```bash
mkdir -p docs
```

### 1d. Module detection

Evaluate these signals in order. The first matching signal decides `mode = "module"`:

1. Two or more `package.json` files exist outside the repo root: `find . -name package.json -not -path './package.json' -not -path './node_modules/*' | wc -l` >= 2
2. Two or more `plugin.json` files exist anywhere: `find . -name plugin.json -not -path './node_modules/*' | wc -l` >= 2
3. A `pnpm-workspace.yaml`, `lerna.json`, or Cargo workspace declaration with >= 2 members exists
4. Two or more top-level directories each contain their own package manifest (`package.json`, `plugin.json`, `Cargo.toml`, `pyproject.toml`, etc.)

If none match, `mode = "feature"`.

Print: `Module detection: <mode> (signal: <which signal matched, or "no signal matched">)`.

### 1e. Enumerate detected modules

Compute `detected_modules`, an ordered list of `{name, path}` objects:

- `mode == "module"`: for each directory matching any signal above, take its basename as `name` and its repo-relative path as `path`. If any `*.md` file exists at the repo root or directly under `docs/` (not under `docs/master-spec/`), also add `{"name": "shared", "path": "."}`.
- `mode == "feature"`: `[{"name": "main", "path": "."}]` (a single module covering the whole repo).

Print: `Modules: <comma-joined list of names>`.

## Step 2: Dispatch cataloger

Dispatch ONE `jupiter-spec-cataloger` agent in the foreground.

Prompt (inline the full text):

```
You are being deployed as the jupiter-spec-cataloger.

mode: <mode>
root: <absolute repo root>
glob_targets: ["docs/**/*.md", "plugins/*/docs/**/*.md", "plugins/*/README.md", "README.md"]
already_adopted: <list from 1b, or [] if fresh>
index_generated_at: <from 1b, or null if fresh>

<inline the full content of plugins/jupiter/agents/jupiter-spec-cataloger.md here>

Return only the JSON catalog, nothing else.
```

Parse the response as JSON. Validate against `plugins/jupiter/schemas/catalog.schema.json`:

```bash
python -c "
import json, sys, jsonschema
schema = json.load(open('plugins/jupiter/schemas/catalog.schema.json'))
data = json.loads(sys.stdin.read())
jsonschema.validate(data, schema)
print('OK')
" <<< "<agent output>"
```

On parse failure: retry ONCE with

```
Your previous response could not be parsed as JSON against catalog.schema.json.
Return ONLY a JSON object matching that schema, no prose, no code fences.
```

On second failure: print the raw output and STOP.

On validation failure: print the offending JSONPath and failing value, then STOP.

## Step 3: Dispatch surface scanner

Use `detected_modules` (from Step 1e) verbatim as the `modules` input. This includes modules that had no specs, so their surface still gets scanned and a `_surface.md` can be generated for them.

Dispatch ONE `jupiter-surface-scanner` agent in the foreground.

Prompt:

```
You are being deployed as the jupiter-surface-scanner.

modules: <JSON list>
deep: <bool>
root: <absolute repo root>

<inline the full content of plugins/jupiter/agents/jupiter-surface-scanner.md here>

Return only the JSON inventory, nothing else.
```

Parse + validate against `plugins/jupiter/schemas/surface.schema.json`. Same retry/STOP policy as Step 2.

## Step 4: Write phase

Compute the bucket iteration set: `all_buckets = union(keys(catalog.proposed_layout), detected_modules[].name)`. A bucket may have zero specs (from `proposed_layout`) and still appear in `all_buckets` if it was a detected module -- in which case step 4b is a no-op for that bucket but step 4c/4d still runs against its surface inventory.

For each bucket in `all_buckets`:

### 4a. Ensure bucket directory

- `mode == "module"`: `mkdir -p docs/master-spec/<bucket>/`
- `mode == "feature"`: `mkdir -p docs/master-spec/features/<bucket>/`

The leaf target path for each spec is:
- `mode == "module"`: `docs/master-spec/<bucket>/<basename>`
- `mode == "feature"`: `docs/master-spec/features/<bucket>/<basename>`

### 4b. Move specs

For each spec path in `catalog.proposed_layout[<bucket>]` (may be an empty list, in which case this step is a no-op for this bucket):

- If `incremental == true` and the spec is already under `docs/master-spec/`, skip the move.
- Compute the target path.
- If target already exists AND points to a different source, rename incoming to `<basename>-2.<ext>` (then `-3`, etc. until no collision). Log: `warn: collision at <target>, renamed incoming to <new-name>`.
- Run: `git mv <source> <target>`

### 4c. Append or refresh surface stubs

For each bucket, iterate the union of `surface.per_module[<bucket>].public_surface` + `surface.per_module[<bucket>].configs` + (if `deep`) `surface.per_module[<bucket>].behavioral_gaps`:

1. For each entry, find the best-matching spec file in that bucket:
   - Score = count of shared topic keywords (case-insensitive) between the spec's `topics` (from the catalog) and the entry's `one_line_summary` split into words + the entry's `name`.
   - Take the spec with the highest score. Ties broken by smallest `loc`.
   - Require score >= 2 to qualify. If no spec qualifies, collect the entry for `_surface.md` (see step 4d).
2. For each qualifying spec, open the file and locate the bounded block:

   ```
   <!-- jupiter:surface-begin -->
   ## Undocumented surface (auto-generated)

   ...

   <!-- jupiter:surface-end -->
   ```

   - If both markers exist: replace the content between them with the fresh list.
   - If neither marker exists: append a new marked block at end of file.
   - If only one marker exists: log `warn: corrupt markers in <path>; skipping surface append`.
3. The rendered content between markers:

   ```
   <!-- jupiter:surface-begin -->
   ## Undocumented surface (auto-generated)

   - **<name>** (`<location>`) - <one_line_summary> <!-- TODO: expand -->
   - **<name>** (`<location>`) - <one_line_summary> <!-- TODO: expand -->
   ...

   <!-- jupiter:surface-end -->
   ```

### 4d. Fallback surface file

If any entries were unmatched in step 4c step 1, write them to `<bucket-dir>/_surface.md`:

```
## Surface gaps for <bucket>

These symbols were found in code but could not be matched to an existing spec.

<!-- jupiter:surface-begin -->
## Undocumented surface (auto-generated)

- **<name>** (`<location>`) - <one_line_summary> <!-- TODO: expand -->
...

<!-- jupiter:surface-end -->
```

If `_surface.md` already exists, apply the same marker-bounded replace/append logic as step 4c.

## Step 5: Write index.json

Compute `docs/master-spec/index.json` from the catalog + surface data:

- `generated_at`: current ISO 8601 timestamp
- `jupiter_version`: read from `plugins/jupiter/.claude-plugin/plugin.json#version`
- `mode`: from Step 1
- `root`: `"docs/master-spec/"`
- `buckets[<name>]`: emit an entry for every bucket in `all_buckets`. A bucket with no specs (e.g., gamma in the multi-module fixture) still appears, with `summary = "surface-only bucket; <K> undocumented entries"` and `files: []` plus one entry for `_surface.md` (see below).
  - `summary`: for buckets that have specs, synthesize from the top 3 topics of the bucket's specs (joined with " / "). For surface-only buckets, use the string `"surface-only bucket; <K> undocumented entries"`.
  - `files[]`: for each spec in the bucket, emit
    ```json
    {
      "path": "<bucket>/<basename>",  // or "features/<bucket>/<basename>"
      "loc": <from catalog>,
      "topics": <from catalog>,
      "sections": <catalog.headings list>,
      "split_candidate": <bool>,
      "split_reason": "<string or absent>",
      "cross_refs": <from catalog>
    }
    ```
    Additionally, if `_surface.md` was written for this bucket (step 4d), append one more entry for it:
    ```json
    {
      "path": "<bucket>/_surface.md",
      "loc": <line count>,
      "topics": ["undocumented-surface"],
      "sections": [],
      "split_candidate": false,
      "cross_refs": []
    }
    ```
  - `gaps_count`: number of surface entries appended to specs in this bucket (including `_surface.md`)
- `split_candidates`: flat list of every `files[].path` where `split_candidate: true`
- `total_loc`: sum of every `files[].loc`
- `scan_summary`: counts across all modules

### Split-candidate rule

`split_candidate = true` iff:
- `loc > 800`, OR
- `sections.length >= 3` AND at least three sections each have `loc >= 150`

Set `split_reason` to a short human-readable string like `"exceeds 800 LOC"` or `"3 top-level sections >=150 LOC each"`. Omit when `split_candidate: false`.

### Validate

Before writing, validate the generated object against `plugins/jupiter/schemas/index.schema.json`. If validation fails, STOP and print the failure.

Write the object to `docs/master-spec/index.json` using 2-space indentation.

## Step 6: Commit

```bash
git add -A
```

Compose the commit message:

- Count `specs_adopted = number of git mv operations performed in Step 4b`.
- Count `buckets_count = all_buckets.length` (the union from Step 4, including surface-only buckets).

```bash
git commit -m "jupiter: adopt <specs_adopted> specs into <buckets_count> buckets (mode: <mode>)"
```

If the pre-commit hook fails:

```
Pre-commit hook failed:
<output>

Continue without committing? (Y/n)
```

On Y: leave changes uncommitted. On n: STOP.

Capture the commit SHA: `commit_sha=$(git rev-parse HEAD)`.

## Step 7: Final summary

Print:

```
jupiter:adopt complete
======================
Mode:              <mode>
Specs adopted:     <N>
Buckets:           <M>
Surface stubs:     <K entries across <M> buckets>
Split candidates:  <J>
Index:             docs/master-spec/index.json
Commit:            <short sha>
```

STOP.
````

- [ ] **Step 2: Verify frontmatter parses as YAML**

```bash
python -c "import yaml,re; t=open('plugins/jupiter/skills/adopt/SKILL.md').read(); m=re.match(r'^---\n(.*?)\n---', t, re.S); print(yaml.safe_load(m.group(1)))"
```

Expected: a dict with keys `name`, `description`, `argument-hint`. No exception.

- [ ] **Step 3: Commit**

```bash
git add plugins/jupiter/skills/adopt/
git commit -m "jupiter: add adopt skill"
```

---

### Task 9: Rewrite skill

**Files:**
- Create: `plugins/jupiter/skills/rewrite/SKILL.md`

- [ ] **Step 1: Write the full skill file**

````markdown
---
name: jupiter:rewrite
description: >
  Consolidate the master spec into a single file (per module in multi-module
  mode), emit a sidecar consolidated-index.json, and prompt the user whether
  to delete the source spec files. Never deletes without explicit consent.
argument-hint: ""
---

You are orchestrating the Jupiter rewrite pipeline. The output is a consolidated document built from the same cataloger + scanner pipeline used by adopt.

## Step 0: Pre-flight

### 0a. Working tree check

Run `git status --porcelain`. If output is non-empty:

```
Warning: working tree has uncommitted changes:
<output>

jupiter:rewrite commits on completion. Continue anyway? (Y/n)
```

Default N. On N, STOP.

### 0b. Module detection

Apply the same detection signals as `jupiter:adopt` Step 1d. Print: `Module detection: <mode> (signal: <which>)`.

### 0c. Enumerate detected modules

Apply the same enumeration as `jupiter:adopt` Step 1e. Store as `detected_modules`. The scanner in Step 1 uses this list for its `modules` input. The write phase in Step 3 uses `all_buckets = union(keys(catalog.proposed_layout), detected_modules[].name)` so that surface-only buckets (like gamma in the multi-module fixture) also get a minimal consolidated file.

## Step 1: Run the cataloger + scanner

Dispatch `jupiter-spec-cataloger` and `jupiter-surface-scanner` exactly as `jupiter:adopt` Steps 2 and 3 do. Use the same prompts, parsing, validation, and retry policy.

Rewrite runs the pipeline fresh every time -- it does not read an existing `docs/master-spec/index.json`. Pass `already_adopted: []` to the cataloger and `deep: false` to the scanner (rewrite has no `--deep` flag).

## Step 2: Plan the consolidation

For each bucket in `all_buckets` (may include buckets that have zero specs but had surface stubs -- they get a minimal consolidated file containing only the surface section):

- Target output file:
  - `mode == "module"`: `docs/master-spec/CONSOLIDATED-<bucket>.md`
  - `mode == "feature"`: `docs/master-spec/CONSOLIDATED.md` (single file across all feature buckets)
- Order specs within the bucket by topic proximity:
  - Build a similarity score between each pair of specs: count of shared topic keywords.
  - Greedy order: start with the spec that has the highest mean similarity to the rest (most central). At each step, append the unplaced spec most similar to the last placed spec. Ties broken by original file `mtime`, oldest first.

## Step 3: Write consolidated files

For each target output file:

1. Open for writing (overwrite if exists).
2. Write the header:

   ```markdown
   # Consolidated Spec: <bucket or "Main">

   Generated by jupiter:rewrite on <ISO 8601 timestamp>.
   Source files are listed in the order shown below; originals preserved unless you chose cleanup at the end of this run.

   ```

3. For each ordered spec, write:

   ```markdown
   <!-- source: <original path> -->
   ## <basename without .md>

   <original file contents, with each `#` heading demoted by one level: `#` -> `##`, `##` -> `###`, etc.>

   ```

4. After all specs in a bucket, append the surface stubs for that bucket using the matching rule from `jupiter:adopt` Step 4c. Inline stubs live inside the same `<!-- jupiter:surface-begin -->` / `<!-- jupiter:surface-end -->` markers, within the consolidated file.
5. In multi-module mode, repeat for each bucket -- one output file per bucket.

## Step 4: Write consolidated-index.json

Compute `docs/master-spec/consolidated-index.json`:

- `generated_at`: current ISO 8601
- `jupiter_version`: from `plugin.json#version`
- `mode`: from Step 0b
- `root`: `"docs/master-spec/"`
- `consolidated_files`: list of every consolidated file written in Step 3
- `buckets[<name>]`:
  - `summary`: top-3 topics joined with " / "
  - `source_file`: the consolidated file that contains this bucket
  - `sections[]`: for each spec in the bucket, emit
    ```json
    {
      "anchor": "#<slugified h2 heading>",
      "original_path": "<original path of the spec>",
      "heading": "## <basename without .md>",
      "line": <line number inside the consolidated file>,
      "loc": <loc of that section in the consolidated file>,
      "topics": <from catalog>
    }
    ```
  - `gaps_count`: number of surface stubs appended to this bucket
- `total_loc`: sum of every consolidated file's LOC
- `scan_summary`: counts across all modules

Slugify: lowercase; replace runs of non-alphanumerics with `-`; trim leading/trailing `-`.

Validate the object against `plugins/jupiter/schemas/consolidated-index.schema.json`. On failure, STOP with the offending JSONPath.

Write to `docs/master-spec/consolidated-index.json` using 2-space indentation.

## Step 5: Cleanup prompt

Compute:
- `total_sources`: number of distinct source spec paths read by the cataloger
- `output_paths`: list of consolidated file paths written in Step 3
- `total_loc`: the value from the index

Prompt:

```
Rewrite complete. <total_sources> source files consolidated into:
  <output path 1>
  <output path 2>
  ...
Total: <total_loc> LOC.

Delete the original spec files now? (y/N)
```

Default N. The default is keep -- never delete on blank input.

### On N (default)

Commit only the consolidated files + sidecar index:

```bash
git add docs/master-spec/
git commit -m "jupiter: rewrite <total_sources> specs into <count> consolidated file(s) (originals kept)"
```

### On y

Run `git rm` on every original spec path from the catalog. Do NOT remove anything outside `catalog.specs[].path`.

```bash
git rm <original path 1> <original path 2> ...
git add docs/master-spec/
git commit -m "jupiter: rewrite + cleanup <total_sources> originals into <count> consolidated file(s)"
```

## Step 6: Final summary

Print:

```
jupiter:rewrite complete
========================
Mode:              <mode>
Sources:           <total_sources>
Consolidated:      <count> file(s)
Total LOC:         <total_loc>
Originals:         <kept | deleted>
Index:             docs/master-spec/consolidated-index.json
Commit:            <short sha>
```

STOP.
````

- [ ] **Step 2: Verify frontmatter parses as YAML**

```bash
python -c "import yaml,re; t=open('plugins/jupiter/skills/rewrite/SKILL.md').read(); m=re.match(r'^---\n(.*?)\n---', t, re.S); print(yaml.safe_load(m.group(1)))"
```

Expected: a dict with keys `name`, `description`, `argument-hint`. No exception.

- [ ] **Step 3: Commit**

```bash
git add plugins/jupiter/skills/rewrite/
git commit -m "jupiter: add rewrite skill"
```

---

### Task 10: Multi-module test fixture

**Files:**
- Create: `plugins/jupiter/test-fixtures/fixture-multi-module/README.md`
- Create: `plugins/jupiter/test-fixtures/fixture-multi-module/.claude-plugin/marketplace.json`
- Create: `plugins/jupiter/test-fixtures/fixture-multi-module/plugins/alpha/.claude-plugin/plugin.json`
- Create: `plugins/jupiter/test-fixtures/fixture-multi-module/plugins/alpha/agents/alpha-scout.md`
- Create: `plugins/jupiter/test-fixtures/fixture-multi-module/plugins/alpha/skills/run/SKILL.md`
- Create: `plugins/jupiter/test-fixtures/fixture-multi-module/plugins/alpha/docs/2026-04-10-design.md`
- Create: `plugins/jupiter/test-fixtures/fixture-multi-module/plugins/beta/.claude-plugin/plugin.json`
- Create: `plugins/jupiter/test-fixtures/fixture-multi-module/plugins/beta/agents/beta-worker.md`
- Create: `plugins/jupiter/test-fixtures/fixture-multi-module/plugins/beta/docs/MASTER-SPEC.md`
- Create: `plugins/jupiter/test-fixtures/fixture-multi-module/plugins/gamma/.claude-plugin/plugin.json`
- Create: `plugins/jupiter/test-fixtures/fixture-multi-module/plugins/gamma/agents/gamma-linter.md`
- Create: `plugins/jupiter/test-fixtures/fixture-multi-module/docs/2026-04-15-shared-design.md`

This fixture exercises multi-module detection, scattered spec locations, gamma having no existing spec (all surface stubs), and a root-level shared spec.

- [ ] **Step 1: Write the fixture README**

`plugins/jupiter/test-fixtures/fixture-multi-module/README.md`:

```markdown
# Fixture: multi-module

Three fake plugins (alpha, beta, gamma) with scattered specs. Mirrors this marketplace's shape.

## Expected adoption outcome (fresh run)

- Mode detected: `module` (three `plugin.json` files)
- Buckets: `alpha`, `beta`, `gamma`, `shared`
- Specs moved:
  - `plugins/alpha/docs/2026-04-10-design.md` -> `docs/master-spec/alpha/2026-04-10-design.md`
  - `plugins/beta/docs/MASTER-SPEC.md` -> `docs/master-spec/beta/MASTER-SPEC.md`
  - `docs/2026-04-15-shared-design.md` -> `docs/master-spec/shared/2026-04-15-shared-design.md`
- Gamma has no existing spec -> `docs/master-spec/gamma/_surface.md` created with `gamma-linter` stub
- `docs/master-spec/index.json` contains 4 buckets, scan_summary.public_surface_found >= 5

## Reset after a smoke test

```bash
git reset --hard HEAD~N
git clean -fd docs/master-spec
```

Where N is the number of commits Jupiter made during the smoke test.
```

- [ ] **Step 2: Write the fixture marketplace registry**

`plugins/jupiter/test-fixtures/fixture-multi-module/.claude-plugin/marketplace.json`:

```json
{
  "name": "jupiter-test-multi",
  "plugins": [
    { "name": "alpha", "source": "./plugins/alpha" },
    { "name": "beta", "source": "./plugins/beta" },
    { "name": "gamma", "source": "./plugins/gamma" }
  ]
}
```

- [ ] **Step 3: Write alpha plugin files**

`plugins/jupiter/test-fixtures/fixture-multi-module/plugins/alpha/.claude-plugin/plugin.json`:

```json
{
  "name": "alpha",
  "description": "Fixture plugin for Jupiter smoke tests",
  "version": "0.0.1"
}
```

`plugins/jupiter/test-fixtures/fixture-multi-module/plugins/alpha/agents/alpha-scout.md`:

```markdown
---
name: alpha-scout
description: Fixture agent that pretends to scout.
model: sonnet
---

Stub agent body.
```

`plugins/jupiter/test-fixtures/fixture-multi-module/plugins/alpha/skills/run/SKILL.md`:

```markdown
---
name: alpha:run
description: Fixture skill.
---

Stub skill body.
```

`plugins/jupiter/test-fixtures/fixture-multi-module/plugins/alpha/docs/2026-04-10-design.md`:

```markdown
# Alpha Design

## Architecture

Alpha is a fixture plugin used by Jupiter's smoke tests to verify multi-module
adoption. It ships an agent called alpha-scout and a skill called alpha:run.

## Goals

- Exist for testing
- Be adopted cleanly into docs/master-spec/alpha/
```

- [ ] **Step 4: Write beta plugin files**

`plugins/jupiter/test-fixtures/fixture-multi-module/plugins/beta/.claude-plugin/plugin.json`:

```json
{
  "name": "beta",
  "description": "Fixture plugin for Jupiter smoke tests",
  "version": "0.0.1"
}
```

`plugins/jupiter/test-fixtures/fixture-multi-module/plugins/beta/agents/beta-worker.md`:

```markdown
---
name: beta-worker
description: Fixture agent that pretends to work.
model: sonnet
---

Stub agent body.
```

`plugins/jupiter/test-fixtures/fixture-multi-module/plugins/beta/docs/MASTER-SPEC.md`:

```markdown
# Beta Master Spec

## Overview

Beta demonstrates a plugin that already has a consolidated MASTER-SPEC.md.
Jupiter should recognize it as the primary spec for the beta bucket.

## Components

### beta-worker

The only agent. Its behavior is covered here.

## Non-goals

- Doing anything real
```

- [ ] **Step 5: Write gamma plugin files (no spec)**

`plugins/jupiter/test-fixtures/fixture-multi-module/plugins/gamma/.claude-plugin/plugin.json`:

```json
{
  "name": "gamma",
  "description": "Fixture plugin with no docs -- surface-only bucket",
  "version": "0.0.1"
}
```

`plugins/jupiter/test-fixtures/fixture-multi-module/plugins/gamma/agents/gamma-linter.md`:

```markdown
---
name: gamma-linter
description: Fixture linting agent.
model: sonnet
---

Stub agent body.
```

- [ ] **Step 6: Write root-level shared spec**

`plugins/jupiter/test-fixtures/fixture-multi-module/docs/2026-04-15-shared-design.md`:

```markdown
# Shared Design

Spec covering concerns that span all three fixture plugins.

## Shared pattern

All three plugins use the same agent manifest pattern.
```

- [ ] **Step 7: Commit**

```bash
git add plugins/jupiter/test-fixtures/fixture-multi-module/
git commit -m "jupiter: add multi-module smoke test fixture"
```

---

### Task 11: Single-module test fixture

**Files:**
- Create: `plugins/jupiter/test-fixtures/fixture-single-module/README.md`
- Create: `plugins/jupiter/test-fixtures/fixture-single-module/package.json`
- Create: `plugins/jupiter/test-fixtures/fixture-single-module/src/main.js`
- Create: `plugins/jupiter/test-fixtures/fixture-single-module/src/auth.js`
- Create: `plugins/jupiter/test-fixtures/fixture-single-module/.env.example`
- Create: `plugins/jupiter/test-fixtures/fixture-single-module/docs/auth-design.md`
- Create: `plugins/jupiter/test-fixtures/fixture-single-module/docs/storage-design.md`
- Create: `plugins/jupiter/test-fixtures/fixture-single-module/docs/ui-design.md`

This fixture exercises feature-mode detection (single `package.json` at root), undocumented `src/auth.js` function surface, and env var config scanning.

- [ ] **Step 1: Write the fixture README**

`plugins/jupiter/test-fixtures/fixture-single-module/README.md`:

```markdown
# Fixture: single-module

Flat repo with one `package.json`, three loose specs, and a small `src/` tree.

## Expected adoption outcome (fresh run)

- Mode detected: `feature` (single root `package.json`, no nested manifests)
- Buckets: feature-keyed, probably `auth`, `storage`, `ui` based on dominant topics
- Specs moved:
  - `docs/auth-design.md` -> `docs/master-spec/features/auth/auth-design.md`
  - `docs/storage-design.md` -> `docs/master-spec/features/storage/storage-design.md`
  - `docs/ui-design.md` -> `docs/master-spec/features/ui/ui-design.md`
- Surface stubs include `DATABASE_URL`, `API_KEY` env vars and any exported
  functions from `src/main.js` + `src/auth.js` not mentioned in any spec

## Reset after a smoke test

```bash
git reset --hard HEAD~N
git clean -fd docs/master-spec
```
```

- [ ] **Step 2: Write root manifest + source files**

`plugins/jupiter/test-fixtures/fixture-single-module/package.json`:

```json
{
  "name": "fixture-single-module",
  "version": "0.0.1",
  "main": "src/main.js"
}
```

`plugins/jupiter/test-fixtures/fixture-single-module/src/main.js`:

```javascript
export function bootstrap() {
  return "fixture main entry";
}

export function healthCheck() {
  return { ok: true };
}
```

`plugins/jupiter/test-fixtures/fixture-single-module/src/auth.js`:

```javascript
export function login(user, password) {
  return { token: "fixture-token" };
}

export function logout(token) {
  return { ok: true };
}

export function rotateKey() {
  return { rotated: true };
}
```

`plugins/jupiter/test-fixtures/fixture-single-module/.env.example`:

```
DATABASE_URL=postgres://localhost/fixture
API_KEY=changeme
LOG_LEVEL=info
```

- [ ] **Step 3: Write the three design specs**

`plugins/jupiter/test-fixtures/fixture-single-module/docs/auth-design.md`:

```markdown
# Auth Design

## Overview

Login and logout behavior for the fixture app. The `login` function returns a
token; `logout` invalidates it.

## Endpoints

- POST /login
- POST /logout

Note: the rotateKey function exists in src/auth.js but is not covered here.
```

`plugins/jupiter/test-fixtures/fixture-single-module/docs/storage-design.md`:

```markdown
# Storage Design

## Overview

How the fixture app persists data. Uses DATABASE_URL from environment.

## Tables

- users
- sessions
```

`plugins/jupiter/test-fixtures/fixture-single-module/docs/ui-design.md`:

```markdown
# UI Design

## Overview

Single-page UI. Loads at / and calls /health on startup.
```

- [ ] **Step 4: Commit**

```bash
git add plugins/jupiter/test-fixtures/fixture-single-module/
git commit -m "jupiter: add single-module smoke test fixture"
```

---

### Task 12: Fleshed-out README with usage + smoke test procedure

**Files:**
- Modify: `plugins/jupiter/README.md` (replace stub)

- [ ] **Step 1: Overwrite README with full content**

```markdown
# Jupiter

Consolidate scattered spec files into a canonical `docs/master-spec/` tree, emit a navigable `index.json`, and append stubs for undocumented public surface found in the code.

## Commands

### `/jupiter:adopt [--force] [--deep]`

Primary command. Reorganizes every spec Jupiter can find into `docs/master-spec/`, organized by module (for multi-module repos) or feature (for single-module repos). Moves files with `git mv` so history is preserved.

- `--force`: wipe `docs/master-spec/` and rebuild (prompts first; default N).
- `--deep`: enable the optional behavioral-gaps pass (pass D) in the surface scanner. Off by default.

Re-running without `--force` is incremental: already-adopted specs are skipped; only new or changed specs are moved; surface stubs inside `<!-- jupiter:surface-begin -->` markers are regenerated; content outside those markers is preserved.

### `/jupiter:rewrite`

Consolidates the master spec into a single file (per module in multi-module mode) and prompts whether to delete the source files. Default answer is keep. Never deletes on blank input.

## Output layout

```
docs/master-spec/
  index.json                             # emitted by /jupiter:adopt
  <module>/<spec>.md                     # multi-module layout
  features/<feature>/<spec>.md           # single-module layout
  _surface.md                            # stubs that matched no existing spec
  CONSOLIDATED.md                        # emitted by /jupiter:rewrite (single-module)
  CONSOLIDATED-<module>.md               # emitted by /jupiter:rewrite (multi-module)
  consolidated-index.json                # rewrite sidecar
```

## Surface stub markers

Jupiter only rewrites content between these markers on incremental re-runs:

```
<!-- jupiter:surface-begin -->
## Undocumented surface (auto-generated)

- **name** (`location`) - summary <!-- TODO: expand -->

<!-- jupiter:surface-end -->
```

Anything above or below is never touched. To promote a stub into real prose, cut it out of the block, expand it above the opening marker, and it will survive future runs.

## Smoke testing

Two fixtures under `test-fixtures/`:

### Multi-module

```bash
cd plugins/jupiter/test-fixtures/fixture-multi-module
claude
## inside claude:
/jupiter:adopt
```

Expected:
- Module detection reports `module`
- `docs/master-spec/index.json` exists and validates against `schemas/index.schema.json`
- Four buckets: `alpha`, `beta`, `gamma`, `shared`
- `docs/master-spec/gamma/_surface.md` exists (no existing spec in gamma)
- One commit named `jupiter: adopt 3 specs into 4 buckets (mode: module)` (or similar)

Incremental re-run:
```bash
## add a new spec
echo "# New alpha spec\n\n## Overview" > plugins/alpha/docs/2026-04-20-extra.md
## inside claude:
/jupiter:adopt
```
Expected: only the new file is moved; existing adopted specs are untouched.

Force:
```bash
## inside claude:
/jupiter:adopt --force
## answer y to the deletion prompt
```
Expected: `docs/master-spec/` wiped and rebuilt from scratch.

### Single-module

```bash
cd plugins/jupiter/test-fixtures/fixture-single-module
claude
## inside claude:
/jupiter:adopt
```

Expected:
- Module detection reports `feature`
- Buckets use `features/<name>/` layout
- `rotateKey`, `DATABASE_URL`, `API_KEY`, and `LOG_LEVEL` appear as surface stubs
- `healthCheck` appears as a stub (not mentioned in ui-design.md)

### Rewrite

```bash
cd plugins/jupiter/test-fixtures/fixture-multi-module
## inside claude:
/jupiter:rewrite
## answer N when prompted -- keep originals
```

Expected:
- Three `docs/master-spec/CONSOLIDATED-*.md` files created (one per non-empty bucket)
- `docs/master-spec/consolidated-index.json` validates against the schema
- Original spec files still present under `plugins/*/docs/`

### Reset between tests

```bash
git reset --hard HEAD~N
git clean -fd docs/master-spec
```

Where N is the number of commits Jupiter made during the test.

## Schema validation

Run the schema test after any schema change:

```bash
python plugins/jupiter/tests/validate_schemas.py
```

Expected: exit 0 with PASS lines for all four schemas.
```

- [ ] **Step 2: Commit**

```bash
git add plugins/jupiter/README.md
git commit -m "jupiter: write full README with usage and smoke test procedure"
```

---

### Task 13: Marketplace registration

**Files:**
- Modify: `.claude-plugin/marketplace.json`
- Modify: `README.md`
- Modify: `CLAUDE.md`

- [ ] **Step 1: Add jupiter to `.claude-plugin/marketplace.json`**

Open `.claude-plugin/marketplace.json`. Find the `"plugins"` array. Append this object after the last existing entry (inside the array):

```json
    {
      "name": "jupiter",
      "description": "Consolidate scattered specs into a canonical master-spec tree with a navigable index, and append stubs for undocumented public surface found in code",
      "source": "./plugins/jupiter",
      "category": "development"
    }
```

Mind the comma: the previously-last entry needs a trailing comma after it.

- [ ] **Step 2: Verify marketplace.json parses**

```bash
python -c "import json; json.load(open('.claude-plugin/marketplace.json'))" && echo OK
```

Expected: `OK`.

- [ ] **Step 3: Add jupiter to root `README.md`**

Find the plugins table in `README.md` (the section that lists `qa-swarm`, `code-atlas`, `plan-runner`). Append this row at the bottom of the table:

```markdown
| `jupiter` | 0.1.0 | Consolidate scattered specs into a canonical master-spec tree with a navigable index, and append stubs for undocumented public surface found in code |
```

If the table columns in the actual README differ (e.g., different column order), match whatever is there.

- [ ] **Step 4: Add jupiter to `CLAUDE.md`**

In `CLAUDE.md`:

1. Find the "Current Plugins" table. Append:

```markdown
| `jupiter` | 0.1.0 | Consolidate scattered specs into a canonical master-spec tree -- adopt command reorganizes in place; rewrite command consolidates to single file with optional cleanup; index.json flags split candidates; surface scanner appends stubs for undocumented agents/skills/CLIs/configs |
```

2. Find the "Directory Layout" code block. Add after the plan-runner entry:

```
  jupiter/.claude-plugin/plugin.json         # manifest (v0.1.0)
```

3. Find the "Directory Map" section. Add after the plan-runner sub-tree:

```
  jupiter/                  # Spec consolidation + codebase surface scanner (v0.1.0)
    .claude-plugin/         # Plugin manifest and metadata
    agents/                 # 2 pipeline agents (spec-cataloger, surface-scanner)
    skills/                 # User-facing commands: adopt, rewrite
    schemas/                # JSON Schemas for agent and index contracts
    test-fixtures/          # Reference fixtures for smoke testing
```

- [ ] **Step 5: Commit**

```bash
git add .claude-plugin/marketplace.json README.md CLAUDE.md
git commit -m "jupiter: register plugin in marketplace and root docs"
```

---

### Task 14: Smoke test -- multi-module baseline

This task is manual. Its purpose is to catch problems the plan did not anticipate.

- [ ] **Step 1: Prepare the fixture as a temporary working directory**

```bash
cp -r plugins/jupiter/test-fixtures/fixture-multi-module /tmp/jupiter-smoke-multi
cd /tmp/jupiter-smoke-multi
git init && git add -A && git commit -m "fixture seed"
```

(Working in a copy avoids polluting the marketplace repo's git history with smoke commits.)

- [ ] **Step 2: Run `/jupiter:adopt`**

Start Claude Code in `/tmp/jupiter-smoke-multi`. Run:

```
/jupiter:adopt
```

Let the pipeline complete.

- [ ] **Step 3: Verify outputs against the fixture README's expected outcome**

Check each of these in the fixture:

1. Module detection reported `mode: module`.
2. `docs/master-spec/index.json` exists.
3. Validate the index:
   ```bash
   python -c "
   import json, jsonschema
   schema = json.load(open('$MARKETPLACE/plugins/jupiter/schemas/index.schema.json'))
   data = json.load(open('docs/master-spec/index.json'))
   jsonschema.validate(data, schema)
   print('OK')
   "
   ```
   where `$MARKETPLACE` is the absolute path to your qa-claude-market checkout. Expected: `OK`.
4. Buckets present: `ls docs/master-spec/` should show `alpha/`, `beta/`, `gamma/`, `shared/`, and `index.json`.
5. `docs/master-spec/gamma/_surface.md` exists and contains a stub for `gamma-linter`.
6. `git log --oneline` shows one commit from Jupiter with message matching `jupiter: adopt <N> specs into 4 buckets (mode: module)`.

- [ ] **Step 4: If any check fails, fix the skill or agent**

If a check fails, the root cause is almost always in `plugins/jupiter/skills/adopt/SKILL.md` or one of the two agent files. Edit the relevant file in the marketplace repo, then re-run `/jupiter:adopt --force` in the smoke fixture (answer y at the wipe prompt) to re-test.

- [ ] **Step 5: Commit any fixes in the marketplace repo**

From the marketplace repo root:

```bash
git add plugins/jupiter/
git commit -m "jupiter: fix <short description> from multi-module smoke test"
```

If no fixes were needed, skip this step.

- [ ] **Step 6: Clean up**

```bash
rm -rf /tmp/jupiter-smoke-multi
```

---

### Task 15: Smoke test -- incremental re-run

- [ ] **Step 1: Prepare a fresh fixture copy and run the baseline adopt**

```bash
cp -r plugins/jupiter/test-fixtures/fixture-multi-module /tmp/jupiter-smoke-incr
cd /tmp/jupiter-smoke-incr
git init && git add -A && git commit -m "fixture seed"
```

Inside Claude Code in that directory:
```
/jupiter:adopt
```

- [ ] **Step 2: Add a new spec and re-run adopt**

Outside Claude:
```bash
cat > plugins/alpha/docs/2026-04-20-extra.md <<EOF
# Alpha Extra Spec

## Purpose

A second design doc for alpha, added after the initial adopt.
EOF
git add -A && git commit -m "add extra alpha spec"
```

Back in Claude Code:
```
/jupiter:adopt
```

- [ ] **Step 3: Verify incremental behavior**

Check:

1. `docs/master-spec/alpha/2026-04-20-extra.md` exists.
2. `docs/master-spec/alpha/2026-04-10-design.md` has the same content as before the re-run (manual edits, if any, preserved). Check its git history: `git log --follow -- docs/master-spec/alpha/2026-04-10-design.md` should show only one "add" commit plus optionally a "jupiter: adopt" commit -- no churn.
3. The new `index.json` includes the extra file in the alpha bucket.
4. Surface-stub markers in existing files are present exactly once (no duplicate marker pairs).

- [ ] **Step 4: Verify marker-preservation**

Manually edit one of the adopted specs to add prose ABOVE the `<!-- jupiter:surface-begin -->` marker. Run `/jupiter:adopt` again. Confirm the added prose survives.

- [ ] **Step 5: Fix any issues, commit in marketplace, clean up**

```bash
rm -rf /tmp/jupiter-smoke-incr
```

---

## Task 16: Smoke test -- adopt --force

- [ ] **Step 1: Prepare a fresh fixture copy and run baseline adopt**

```bash
cp -r plugins/jupiter/test-fixtures/fixture-multi-module /tmp/jupiter-smoke-force
cd /tmp/jupiter-smoke-force
git init && git add -A && git commit -m "fixture seed"
```

Inside Claude Code: `/jupiter:adopt`.

- [ ] **Step 2: Manually modify the adopted tree**

Outside Claude:
```bash
echo "" >> docs/master-spec/alpha/2026-04-10-design.md
echo "## A manual section" >> docs/master-spec/alpha/2026-04-10-design.md
git add -A && git commit -m "manual edit"
```

- [ ] **Step 3: Run `/jupiter:adopt --force`, decline the prompt**

In Claude:
```
/jupiter:adopt --force
```

When prompted `Delete and rebuild? (y/N)`, answer `N`.

Expected: the pipeline STOPs, `docs/master-spec/` is unchanged, manual section still present.

- [ ] **Step 4: Run `/jupiter:adopt --force`, accept**

```
/jupiter:adopt --force
```

Answer `y`.

Expected: `docs/master-spec/` is wiped, rebuilt from scratch. The manual section is gone. `index.json` is freshly generated.

- [ ] **Step 5: Fix any issues, commit in marketplace, clean up**

```bash
rm -rf /tmp/jupiter-smoke-force
```

---

## Task 17: Smoke test -- rewrite (both branches)

- [ ] **Step 1: Prepare a fresh fixture copy**

```bash
cp -r plugins/jupiter/test-fixtures/fixture-multi-module /tmp/jupiter-smoke-rewrite
cd /tmp/jupiter-smoke-rewrite
git init && git add -A && git commit -m "fixture seed"
```

Do NOT run adopt first. Rewrite should work stand-alone.

- [ ] **Step 2: Run `/jupiter:rewrite`, answer N to cleanup**

In Claude:
```
/jupiter:rewrite
```

At the `Delete the original spec files now? (y/N)` prompt, answer `N`.

- [ ] **Step 3: Verify kept-originals branch**

Check:

1. `docs/master-spec/CONSOLIDATED-alpha.md` exists and contains the body of `plugins/alpha/docs/2026-04-10-design.md` (with its `#` heading demoted to `##`).
2. `docs/master-spec/CONSOLIDATED-beta.md` exists.
3. `docs/master-spec/CONSOLIDATED-gamma.md` exists as a minimal file containing only the surface section for gamma's `gamma-linter` agent (no source spec content, because gamma has no specs).
4. `docs/master-spec/CONSOLIDATED-shared.md` exists.
5. `docs/master-spec/consolidated-index.json` exists and validates against `consolidated-index.schema.json`.
6. All original spec files are still present under `plugins/*/docs/` and `docs/`.
7. One commit with message matching `jupiter: rewrite <N> specs into <M> consolidated file(s) (originals kept)`.

- [ ] **Step 4: Run `/jupiter:rewrite` again, answer y**

```
/jupiter:rewrite
```

At the prompt, answer `y`.

- [ ] **Step 5: Verify cleanup branch**

Check:

1. `CONSOLIDATED-*.md` files are refreshed (timestamp updated).
2. Original spec files are deleted from `plugins/*/docs/` and `docs/`.
3. `git status` is clean.
4. Commit message matches `jupiter: rewrite + cleanup <N> originals into <M> consolidated file(s)`.

- [ ] **Step 6: Fix any issues, commit in marketplace, clean up**

```bash
rm -rf /tmp/jupiter-smoke-rewrite
```

---

## Task 18: Smoke test -- single-module fixture

- [ ] **Step 1: Prepare a fresh copy of the single-module fixture**

```bash
cp -r plugins/jupiter/test-fixtures/fixture-single-module /tmp/jupiter-smoke-single
cd /tmp/jupiter-smoke-single
git init && git add -A && git commit -m "fixture seed"
```

- [ ] **Step 2: Run `/jupiter:adopt`**

In Claude: `/jupiter:adopt`.

- [ ] **Step 3: Verify feature-mode outputs**

Check:

1. Module detection reports `mode: feature`.
2. Layout: `docs/master-spec/features/<bucket>/<spec>.md` rather than `docs/master-spec/<bucket>/`.
3. `docs/master-spec/index.json` has `mode: "feature"`.
4. Surface stubs include these expected entries somewhere in the master spec tree (either in an existing spec or `_surface.md`):
   - `rotateKey` (from `src/auth.js`)
   - `healthCheck` (from `src/main.js`)
   - `bootstrap` (from `src/main.js`)
   - `DATABASE_URL` env var
   - `API_KEY` env var
   - `LOG_LEVEL` env var
5. `index.json` validates against the schema.

- [ ] **Step 4: Run `/jupiter:adopt --deep` (optional behavioral pass)**

```
/jupiter:adopt --deep
```

Verify that `scan_summary.behavioral_gaps_found` is >= 0 and the index still validates. The source files are simple enough that this pass may produce zero gaps -- that is fine.

- [ ] **Step 5: Fix any issues, commit in marketplace, clean up**

```bash
rm -rf /tmp/jupiter-smoke-single
```

---

## Task 19: Version tag and release notes

- [ ] **Step 1: Confirm the manifest version**

```bash
python -c "import json; print(json.load(open('plugins/jupiter/.claude-plugin/plugin.json'))['version'])"
```

Expected: `0.1.0`.

If smoke-test fixes bumped any behavior incompatibly, bump to `0.1.1` or similar first and commit.

- [ ] **Step 2: Verify the schema tests still pass**

```bash
python plugins/jupiter/tests/validate_schemas.py
```

Expected: exit 0. PASS for all four schemas.

- [ ] **Step 3: Write a release note entry** (optional -- only if this repo has a CHANGELOG)

Check: `ls CHANGELOG.md 2>/dev/null`. If present, add an entry under a new `## jupiter v0.1.0` heading. If absent, skip.

- [ ] **Step 4: Tag the release**

```bash
git tag jupiter/v0.1.0
```

The user pushes tags with `git push origin --tags` when ready. Do NOT push automatically.

- [ ] **Step 5: Final summary**

Print to the conversation:

```
Jupiter v0.1.0 implementation complete.
- 2 skills: /jupiter:adopt, /jupiter:rewrite
- 2 agents: jupiter-spec-cataloger, jupiter-surface-scanner
- 4 JSON schemas, all validation tests passing
- 2 smoke-test fixtures verified
- Registered in marketplace.json, root README, and CLAUDE.md
- Tagged locally as jupiter/v0.1.0 (push tags when ready)
```


<!-- jupiter:surface-begin -->
## Undocumented surface (auto-generated)

- **jupiter-spec-cataloger** (`plugins/jupiter/agents/jupiter-spec-cataloger.md`) - Reads spec files under docs/ dirs, extracts metadata, proposes bucket layout, returns JSON catalog. <!-- TODO: expand -->
- **jupiter-surface-scanner** (`plugins/jupiter/agents/jupiter-surface-scanner.md`) - Scans repo code per module to inventory public surface and configuration; returns strict JSON. <!-- TODO: expand -->
- **jupiter:adopt** (`plugins/jupiter/skills/adopt/SKILL.md`) - Adopt scattered spec files into canonical docs/master-spec/ tree; emits index.json with stubs. <!-- TODO: expand -->
- **jupiter:rewrite** (`plugins/jupiter/skills/rewrite/SKILL.md`) - Consolidate master spec into a single file per module; emits consolidated-index.json sidecar. <!-- TODO: expand -->
- **catalog.schema.json** (`plugins/jupiter/schemas/catalog.schema.json`) - JSON Schema for the Jupiter spec catalog output produced by the spec-cataloger agent. <!-- TODO: expand -->
- **consolidated-index.schema.json** (`plugins/jupiter/schemas/consolidated-index.schema.json`) - JSON Schema for the consolidated spec index sidecar emitted by the rewrite skill. <!-- TODO: expand -->
- **index.schema.json** (`plugins/jupiter/schemas/index.schema.json`) - JSON Schema for the master spec index emitted by the adopt skill. <!-- TODO: expand -->
- **surface.schema.json** (`plugins/jupiter/schemas/surface.schema.json`) - JSON Schema for the surface inventory output produced by the surface-scanner agent. <!-- TODO: expand -->
- **keywords** (`plugins/jupiter/.claude-plugin/plugin.json`) - Non-default manifest field listing SEO/discovery keywords for the plugin. <!-- TODO: expand -->
- **repository** (`plugins/jupiter/.claude-plugin/plugin.json`) - Non-default manifest field pointing to the plugin's GitHub repository URL. <!-- TODO: expand -->

<!-- jupiter:surface-end -->
